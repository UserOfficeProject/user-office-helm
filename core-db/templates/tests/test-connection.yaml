apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "duo-db.fullname" . }}-test-connection"
  labels:
    {{- include "duo-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "duo-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
