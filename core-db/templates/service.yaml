apiVersion: v1
kind: Service
metadata:
  name: {{ include "duo-db.fullname" . }}
  labels:
    {{- include "duo-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "duo-db.selectorLabels" . | nindent 4 }}
